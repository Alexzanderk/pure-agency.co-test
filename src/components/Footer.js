import React from 'react';

const Footer = () => {
	return (
		<footer style={{ padding: '5px', textAlign: 'center' }}>
			<h1 style={{ margin: 0 }}>FOOTER</h1>
			<p style={{ color: 'grey' }}>
				I didn't have enough time for implement to the end: 1 .change
				opacity when element dragging, 2. remove item in the element
				after dragging 3. controls add delete element
			</p>
		</footer>
	);
};

export default Footer;
